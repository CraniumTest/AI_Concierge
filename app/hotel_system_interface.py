def book_room(user_info, room_type, dates):
    # Simulated booking process
    return f"Room of type {room_type} booked for {user_info['name']} from {dates['check_in']} to {dates['check_out']}."

# Additional simulated interfaces can be developed as needed
